# Jira API Configuration
JIRA_TOKEN=your-jira-personal-access-token-here
JIRA_HOST=https://issues.redhat.com

# Optional: override API port (default: 3001)
API_PORT=3001

# Firebase
VITE_FIREBASE_API_KEY=your-firebase-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-app.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id

# S3 Storage (optional — for deployed mode)
# When BUG_DATA_S3_BUCKET is set, the app reads data from S3 instead of local files
# and the /api/refresh endpoint is disabled. Use `npm run refresh` to update S3 data.
BUG_DATA_S3_BUCKET=
BUG_DATA_S3_PREFIX=
